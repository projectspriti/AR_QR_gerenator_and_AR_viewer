# Backend Environment Variables

# Server Port (Render will set this automatically)
PORT=5000

# Backend URL - Your deployed Render backend URL
BACKEND_URL=https://ar-viewer.onrender.com

# Frontend URL(s) - For CORS configuration
# Production Vercel frontend URL (comma-separated for multiple origins)
# Include both production and local development URLs
FRONTEND_URL=https://arviewer-five.vercel.app,http://localhost:5173

# Node Environment
NODE_ENV=production